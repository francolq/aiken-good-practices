import Properties.Order.Validator
import Properties.Order.MintValidator
import Properties.Order.Soundness
import Properties.Order.Robustness

/-! Aggregator module. Importing this file (or `Properties`, which
    re-exports it) is enough to type-check every theorem in the
    project. -/
